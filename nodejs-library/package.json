{
  "name": "placeholder",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "./build"
  ],
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ext ts --quiet src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "pre-commit": [
    "lint",
    "test",
    "build"
  ]
}
